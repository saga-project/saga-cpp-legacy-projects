# execution of this file fails (because the interpreter is not
# specified on the first line) and commands are not executed via shell
# by default see the implementation of ExecutableApplication using
# subprocess.call() 

echo byebye
