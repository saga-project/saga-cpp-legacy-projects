## -*-python-*-

import THIS as application

import os,shutil

def run(input,config):
    input.data.basedir =  os.path.expanduser('~/diane/apps/THIS/basedir')
    
    for fn in ['input','output','rndm']:
        application.logger.info('THIS:copying %s files'%fn)
        shutil.copytree(os.path.join(input.data.basedir,fn),fn)
